# 🐧 Лабораторна робота №4: Команди Linux для управління процесами
**👨‍💻 Виконали:** Волосковець Дмитро та Перевишко Денис

---

## 📑 Зміст
1. [Словник базових термінів](#словник-базових-термінів)
2. [Відповіді на теоретичні питання](#відповіді-на-теоретичні-питання)
3. [Початкова робота в CLI-режимі](#початкова-робота-в-cli-режимі)
4. [Практична частина: Робота з процесами](#практична-частина-робота-з-процесами)
5. [Контрольні запитання](#контрольні-запитання)
6. [Висновок / Conclusion](#висновок)

---

## 📖 Словник базових термінів

| Термін | Переклад | Пояснення |
|:---|:---|:---|
| **Process** | Процес | Програма, що виконується в даний момент. |
| **PID** | Process ID | Унікальний цифровий ідентифікатор процесу. |
| **Foreground** | Передній план | Процес, що займає термінал до завершення. |
| **Background** | Фоновий режим | Процес, що працює паралельно з іншими задачами. |
| **CPU Usage** | Навантаження CPU | Відсоток потужності процесора, що використовується. |

---

## 📝 Відповіді на теоретичні питання

### 2.1 Моніторинг процесів
Основні інструменти: `ps`, `top`, `htop`, `uptime`. Перегляд параметрів здійснюється через `man ps` або `ps --help`.

### 2.2 Сортування в команді top
Утиліта `top` дозволяє інтерактивне сортування:
- **P** — за навантаженням на процесор (%CPU).
- **M** — за використанням пам'яті (%MEM).
- **N** — за номером PID.



<p align="center">
  <img src="https://i.ibb.co/VpHdN45t/image.png" width="30%" />
  <img src="https://i.ibb.co/PvwpNST8/image.png" width="30%" />
  <img src="https://i.ibb.co/KcSwvbfN/image.png" width="30%" />
</p>

---

## 📂 Початкова робота в CLI-режимі

### Директорія /proc
Це віртуальна файлова система, що слугує інтерфейсом до структур даних ядра.
`ls /proc` — виведення вмісту.
<p align="center">
  <img src="https://i.ibb.co/rKqZLzVW/image.png" width="70%" />
</p>

### Поточні сеанси користувачів
Використовуються команди `who` та `w`.
<a href="https://ibb.co/Q7Yfz5R7"><img src="https://i.ibb.co/RGQ9Z5mG/image.png" alt="image" border="0"></a>
<a href="https://ibb.co/qFJ4vPR2"><img src="https://i.ibb.co/tPLN9Wb1/image.png" alt="image" border="0"></a>

### Системні комбінації клавіш
- **Ctrl + C**: Перериває (завершує) активний процес.
- **Ctrl + Z**: Призупиняє процес та відправляє його у фон.
- **Ctrl + D**: Сигнал EOF (кінець введення) або вихід із сеансу.



---

## 🛠 Практична частина (робота з процесами)

### Аналіз та призупинення top
Запуск `top` та подальше призупинення через `Ctrl + Z`:
<p align="center">
  <img src="https://i.ibb.co/tpj6Qx9q/image.png" width="70%" />
</p>

### Робота з командою ps
Виведення статичного знімку процесів:
<p align="center">
  <img src="https://i.ibb.co/GvYYPndz/image.png" width="70%" />
</p>

### Приклади використання `ps`:
1. **Ієрархія процесів:** `ps -ejH`
2. **Повний формат:** `ps -ef`
3. **Системні процеси:** `ps -A`

<p align="center">
  <img src="https://i.ibb.co/1fRhX0GR/image.png" width="32%" />
  <img src="https://i.ibb.co/XfPCzgmQ/image.png" width="32%" />
  <img src="https://i.ibb.co/Wjr82qV/image.png" width="32%" />
</p>

### Керування фоновими задачами (`jobs`, `fg`, `bg`)
Перегляд активних задач та маніпуляція ними:
<a href="https://ibb.co/ZwFF1yq"><img src="https://i.ibb.co/G1883gz/image.png" alt="image" border="0"></a>

Керування фоновим процесом

### Запуск тестового процесу

```bash
sleep 300
```

### Призупинення процесу

Натиснути:

```
Ctrl + Z
```

Система покаже:

```
[1]+  Stopped  sleep 300
```

---

### Відновлення процесу у foreground

```bash
fg %1
```

---

### Повторне призупинення

```
Ctrl + Z
```

---

### Відновлення процесу у background

```bash
bg %1
```

---

### Завершення процесу

<a href="https://ibb.co/ycCj17T0"><img src="https://i.ibb.co/xtbT0dvh/image.png" alt="image" border="0"></a>

---

## ❓ Контрольні запитання

---

## 1. Призначення директорії `/proc` в Linux

`/proc` — це віртуальна файлова система, яка створюється ядром Linux.

Вона:
- містить інформацію про всі процеси
- відображає стан ядра
- показує системні ресурси в реальному часі

### Основні файли:

- `/proc/PID/` — інформація про конкретний процес
- `/proc/cpuinfo` — дані про процесор
- `/proc/meminfo` — дані про оперативну пам’ять
- `/proc/uptime` — час роботи системи

Дані генеруються динамічно ядром.

---

## 2. Як визначити процес, що використовує найбільше пам’яті

### Спосіб 1 — команда `top`

```bash
top
```

Натиснути:

```
Shift + M
```

Колонка `%MEM` показує відсоток використання оперативної пам’яті.

---

### Спосіб 2 — команда `ps`

```bash
ps aux --sort=-%mem | head -3
```

Показує три процеси з найбільшим використанням пам’яті.

---

## 3. Ієрархія батьківських процесів

### Отримання дерева процесів

```bash
pstree
```

Або:

```bash
ps -ejH
```

### Приклад структури:

```
systemd(1)
 ├─gnome-shell
 │   ├─gnome-terminal
 │   │   └─bash
 │   │       └─sleep
```

### Характеристика:

- PID 1 — `systemd` (кореневий процес)
- Кожен процес має PPID (Parent Process ID)
- Усі процеси формують ієрархічне дерево

---

## 4. Відмінність `top` від `ps`

| `ps` | `top` |
|------|-------|
| Разовий знімок стану системи | Динамічне оновлення |
| Неінтерактивна команда | Інтерактивна |
| Менше навантаження | Постійний моніторинг |
| Не дозволяє керувати процесами | Дозволяє завершувати процеси |

---

## 5. Додаткові можливості `htop` порівняно з `top`

`htop` має:
- Кольоровий інтерфейс
- Підтримку миші
- Зручне сортування
- Дерево процесів
- Просте завершення процесів (F9)
- Відображення навантаження по ядрах CPU
- Прокрутку списку процесів

---

# Моніторинг процесів у мобільній ОС (Android)

---

## 6. Компоненти мобільної ОС для моніторингу процесів

Android базується на ядрі Linux.

Основні компоненти:
- Activity Manager
- Process Manager
- Low Memory Killer
- Файлова система `/proc`
- Developer Options

---

## 7. Термінальне керування процесами в Android

### Через ADB:

```bash
adb shell
ps
top
kill PID
```

### Через Termux

Без root — обмежене керування.  
З root — повний контроль над процесами.

---

## 8. Сторонні засоби моніторингу процесів

Можна встановити:
- Termux
- Системні монітори з Google Play
- Kernel Adiutor (потрібен root)
- htop (через Termux)

З правами root доступні:
- зміна пріоритету процесів (nice)
- примусове завершення
- повний контроль над системними процесами

---

## ✅ Висновок

У ході виконання лабораторної роботи було вивчено принципи роботи з процесами в операційній системі Linux та засоби їх моніторингу. Було отримано практичні навички перегляду списку процесів, визначення найбільш ресурсомістких задач, призупинення та відновлення їх виконання, а також завершення роботи процесів різними способами.

---

## ✅ Conclusion

During this laboratory work, the principles of working with processes in the Linux operating system and the tools for monitoring them via the command-line interface were studied. Practical skills were acquired in viewing the list of processes, identifying resource-intensive tasks, suspending and resuming their execution, and terminating processes using different commands.

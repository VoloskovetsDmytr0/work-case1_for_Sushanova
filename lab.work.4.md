# Лабораторна робота №4: Команди Linux для управління процесами
**Виконали:** Волосковець Дмитро та Перевишко Денис

# Попередня підготовка до лабораторної роботи (Linux, процеси)

## Невеличкий словник базових англійських термінів (команди та параметри)

| Термін | Переклад | Пояснення |
|-------|----------|-----------|
| process | процес | Запущена програма в системі |
| PID (Process ID) | ідентифікатор процесу | Унікальний номер процесу |
| process state | стан процесу | Поточний стан (running, sleeping тощо) |
| foreground | передній план | Процес, який виконується у терміналі |
| background | фоновий режим | Процес, що працює без блокування терміналу |
| monitoring | моніторинг | Відстеження стану процесів |
| parameters (options) | параметри | Додаткові ключі команди |
| terminate | завершити | Завершення роботи процесу |
| suspend | призупинити | Тимчасова зупинка процесу |
| resume | відновити | Продовження виконання процесу |
| job | задача | Процес, запущений у shell |
| memory usage | використання пам’яті | Обсяг спожитої ОЗП |
| CPU usage | використання CPU | Навантаження на процесор |

---

# Відповіді на теоретичні питання

## 2.1. Які команди для моніторингу стану процесів ви знаєте? Як переглянути їх можливі параметри?

Основні команди моніторингу процесів у Linux:
- `ps` — відображає список процесів
- `top` — динамічний моніторинг процесів у реальному часі
- `htop` — розширений інтерактивний монітор процесів
- `atop` — розширений системний монітор
- `uptime` — показує навантаження системи
- `free` — використання пам’яті
- `watch` — періодичний запуск команд

Перегляд можливих параметрів:
```bash
man ps
ps --help
man top
top --help
````

Команда `man` відкриває довідкову сторінку з повним описом параметрів.

---

## 2.2. Чи може команда ps у реальному часі відслідковувати стан процесів?

Ні, команда `ps` не відслідковує процеси у реальному часі.
Вона показує знімок (snapshot) стану процесів на момент виконання команди.
Для реального часу використовується команда `top` або `htop`.

---

## 2.3. За якими параметрами можливе сортування процесів в команді top? Як переключатись між ними?

У команді `top` процеси можна сортувати за:

* %CPU — використання процесора
* %MEM — використання пам’яті
* PID — ідентифікатор процесу
* TIME+ — час роботи процесу
* USER — користувач
* VIRT / RES — пам’ять

Переключення сортування:

* `P` — сортування за CPU
* `M` — сортування за пам’яттю
* `T` — за часом роботи
* `N` — за PID
* `<` або `>` — зміна поля сортування

(скрін роботи команди top і сортування — вставити після цього пункту)

---

## 2.4. Які команди для завершення роботи процесів ви знаєте?

Основні команди:

* `kill PID` — завершення процесу за PID
* `kill -9 PID` — примусове завершення
* `pkill ім'я_процесу` — завершення за назвою
* `killall ім'я_процесу` — завершення всіх процесів з такою назвою
* `xkill` — завершення через графічний інтерфейс

---

# Початкова робота в CLI-режимі в Linux

## Як вивести вміст директорії /proc? Де вона знаходиться та для чого призначена?

Команда:

```bash
ls /proc
```

(скрін вмісту /proc — вставити тут)

Директорія `/proc` знаходиться у кореневій файловій системі (`/`).
Вона є віртуальною файловою системою, що містить інформацію про:

* процеси (каталоги з номерами PID)
* ядро системи
* використання пам’яті
* процесор
* системні параметри

Інформація в `/proc`:

* `/proc/cpuinfo` — інформація про CPU
* `/proc/meminfo` — інформація про пам’ять
* `/proc/[PID]` — дані про конкретний процес

---

## Як вивести інформацію про поточні сеанси користувачів?

Команди:

```bash
who
w
users
```

Найчастіше використовується `who` або `w`.

(скрін виконання команди who — вставити тут)

---

## Які дії можна зробити в терміналі за допомогою Ctrl + C, Ctrl + D та Ctrl + Z?

* `Ctrl + C` — примусове завершення поточного процесу
* `Ctrl + D` — вихід з терміналу або завершення введення (EOF)
* `Ctrl + Z` — призупинення процесу (переведення у background suspended)

---

## Чим відрізняється фоновий процес від звичайного. Де вони використовуються?

Звичайний (foreground) процес:

* Виконується у поточному терміналі
* Блокує командний рядок

Фоновий (background) процес:

* Працює у фоновому режимі
* Не блокує термінал
* Запускається через `&`

Приклад:

```bash
sleep 100 &
```

Використання:

* серверні служби
* довгі обчислення
* завантаження файлів
* автоматизація задач

---

## Опишіть команди jobs, bg, fg

* `jobs` — показує список фонових та призупинених задач
* `bg` — відновлює процес у фоновому режимі
* `fg` — переводить процес на передній план

---

## Якою командою можна переглянути інформацію про запущені фонові процеси та задачі?

```bash
jobs
ps
top
```

(скрін команди jobs — вставити тут)

---

## Як призупинити фоновий процес, як його потім відновити та при необхідності перезапустити?

1. Призупинити:

```bash
Ctrl + Z
```

2. Відновити у фоні:

```bash
bg %номер_задачі
```

3. Відновити на передньому плані:

```bash
fg %номер_задачі
```

4. Перезапуск:

```bash
kill PID
./program &
```

---

# Практична частина (робота з процесами)

## Аналіз команди top

Команда:

```bash
top
```

Вона показує:

* активні процеси
* навантаження CPU
* використання RAM
* час роботи системи

Найбільш активні процеси — ті, що мають найбільший %CPU або %MEM.
(скрін top — вставити тут)

---

## Призупинення виконання команди top

Комбінація:

```bash
Ctrl + Z
```

(скрін призупинення top — вставити тут)

---

## Виведення інформації про процеси за допомогою ps

```bash
ps
```

(скрін ps — вставити тут)

---

## 5 прикладів використання команди ps

1. Всі процеси:

```bash
ps aux
```

Показує всі процеси системи.

2. Процеси конкретного користувача:

```bash
ps -u username
```

Виводить процеси заданого користувача.

3. Дерево процесів:

```bash
ps -ejH
```

Показує ієрархію процесів.

4. Детальна інформація:

```bash
ps -ef
```

Повний формат відображення процесів.

5. Системні процеси:

```bash
ps -A
```

Показує всі системні процеси.

(скріни кожної команди ps — вставити після кожного прикладу)

---

## Чи є запущені фонові процеси?

Команда:

```bash
jobs
```

(скрін jobs — вставити тут)

---

## Відновлення призупиненого процесу

1. На передній план:

```bash
fg %1
```

(скрін fg — вставити тут)

2. Знову призупинити:

```bash
Ctrl + Z
```

3. У фоновий режим:

```bash
bg %1
```

(скрін bg — вставити тут)

4. Завершення фонового процесу:

```bash
kill %1
```

(скрін завершення процесу — вставити тут)

---

# Контрольні запитання

## Яке призначення директорії /proc в системах Linux?

`/proc` — це віртуальна файловa система, що містить інформацію про:

* процеси
* ядро
* пам’ять
* апаратні ресурси
* системні параметри

---

## Як серед будь-яких трьох процесів визначити, який використовує найбільше пам’яті?

Команда:

```bash
top
```

або

```bash
ps aux --sort=-%mem
```

Порівнюємо значення `%MEM` та `RES`.
Відсоток пам’яті показується у колонці `%MEM`.

(скрін сортування по пам’яті — вставити тут)

---

## Як отримати ієрархію батьківських процесів в Linux?

Команди:

```bash
pstree
ps -ejH
```

Структура:

```
init (PID 1)
 ├─ systemd
 │   ├─ bash
 │   │   └─ top
```

Батьківський процес створює дочірні процеси.

---

## Чим відрізняється команда top від ps?

| top                        | ps                |
| -------------------------- | ----------------- |
| Динамічний моніторинг      | Статичний знімок  |
| Оновлення в реальному часі | Одноразовий вивід |
| Інтерактивний              | Неінтерактивний   |

---

## Які додаткові можливості реалізує htop в порівнянні з top?

* Зручний інтерфейс
* Кольорове відображення
* Керування мишею
* Пошук процесів
* Прокрутка списку
* Легке завершення процесів (F9)

---

## Компоненти мобільної ОС для моніторингу процесів

У мобільних ОС (Android/iOS):

* Диспетчер задач
* Монітор використання батареї
* Системний монітор ресурсів
* Налаштування → Програми → Активні процеси

---

## Чи підтримує мобільна ОС термінальне керування процесами?

Android:

* Так, через Termux або ADB shell
* Команди: `ps`, `top`, `kill`

iOS:

* Обмежено (без jailbreak)
* Через системні налаштування

---

## Чи можливо встановити сторонні засоби моніторингу процесів на телефон?

Так, можна встановити:

* Termux — термінал Linux для Android
* System Monitor apps
* ADB tools
* Task Manager додатки

Вони дозволяють:

* моніторити CPU та RAM
* переглядати процеси
* завершувати задачі
* аналізувати продуктивність системи

```
```

# 🐧 Лабораторна робота №6: "Команди Linux для архівування та стиснення даних. Робота з текстом"

**👨‍💻 Виконали:** Волосковець Дмитро та Перевишко Денис  

---

# Словник термінів

**Архівування** — процес об'єднання декількох файлів або каталогів в один архівний файл.

**Стиснення** — процес зменшення розміру файлу за допомогою спеціальних алгоритмів.

**Командна оболонка (Shell)** — програма, що дозволяє користувачу взаємодіяти з операційною системою за допомогою текстових команд.

**Потоки (Streams)** — канали передачі даних між програмами.

**Перенаправлення (Redirection)** — зміна напрямку стандартних потоків введення або виведення.

**Pipe (канал)** — механізм передачі результату виконання однієї команди як вхідних даних для іншої.

---

# Команди архівування та стискання

## tar

Команда `tar` використовується для створення архівів, що містять декілька файлів або каталогів.

| Параметр | Призначення |
|---|---|
| -c | створити архів |
| -x | розпакувати архів |
| -v | детальний режим |
| -f | вказати ім'я файлу |
| -t | переглянути вміст архіву |

Приклад:

```bash
tar -cvf archive.tar folder
```

Встановлення:

```bash
sudo apt install tar
```

---

## gzip

Команда `gzip` використовується для стискання файлів.

```bash
gzip file.txt
```

Основні параметри

| Параметр | Опис |
|---|---|
| -d | розпакування |
| -k | зберегти оригінал |

Встановлення:

```bash
sudo apt install gzip
```

---

## bzip2

Команда `bzip2` забезпечує більш сильне стискання файлів.

```bash
bzip2 file.txt
```

Встановлення:

```bash
sudo apt install bzip2
```

---

## xz

Команда `xz` забезпечує дуже ефективне стискання файлів.

```bash
xz file.txt
```

Встановлення:

```bash
sudo apt install xz-utils
```

---

## zip

Команда `zip` використовується для створення zip-архівів.

```bash
zip archive.zip file.txt
```

Встановлення:

```bash
sudo apt install zip
```

---

# Приклади архівування та стискання

```bash
tar -cvf backup.tar folder
```

```bash
tar -czvf backup.tar.gz folder
```

```bash
tar -cjvf backup.tar.bz2 folder
```

---

# Команди роботи з текстом

## cat

Виводить вміст файлу.

```bash
cat file.txt
```

---

## less

Перегляд тексту посторінково.

```bash
less file.txt
```

---

## more

Перегляд файлу сторінками.

```bash
more file.txt
```

---

## head

Показує перші рядки файлу.

```bash
head file.txt
```

---

## tail

Показує останні рядки файлу.

```bash
tail file.txt
```

---

# Команда grep

Команда `grep` використовується для пошуку тексту у файлах.

```bash
grep "text" file.txt
```

Основні параметри

| Параметр | Опис |
|---|---|
| -i | ігнорувати регістр |
| -r | рекурсивний пошук |
| -n | номер рядка |

---

# Перенаправлення потоків

| Команда | Що виконує |
|---|---|
| cmd 1> file | перенаправлення стандартного виводу |
| cmd > file | те саме що і 1> |
| cmd 2> file | перенаправлення помилок |
| cmd >> file | додавання у файл |
| cmd &> file | stdout + stderr |
| cmd > file 2>&1 | об'єднання потоків |
| cmd >> file 2>&1 | додавання двох потоків |
| cmd 2>&1 > /dev/null | помилки у stdout |
| cmd 2> /dev/null | приховування помилок |
| cmd1 \| cmd2 | pipe між командами |
| cmd1 2>&1 \| cmd2 | pipe з помилками |

---

# Приклади команд

| Команда | Що виконує |
|---|---|
| echo "It is a new story." > story | запис тексту у файл |
| date > date.txt | запис дати |
| cat file1 file2 file3 > bigfile | об'єднання файлів |
| ls -l >> directory | додавання списку файлів |
| sort < file1_unsorted > file2_sorted | сортування |
| find -name '*.txt' > file.txt 2> /dev/null | пошук файлів |
| cat file1_unsorted \| sort > file2_sorted | pipe |
| cat myfile \| grep student \| wc -l | підрахунок рядків |

---

# Скріншоти виконання команд

## Створення директорії

Виконати:

```bash
mkdir mybackups
```

(ТУТ ВСТАВИТИ СКРІНШОТ)

---

## Створення tar архіву

```bash
tar -cvf mybackups/backup.tar /etc
```

(ТУТ ВСТАВИТИ СКРІНШОТ)

---

## Перегляд архіву

```bash
tar -tvf mybackups/backup.tar
```

(ТУТ ВСТАВИТИ СКРІНШОТ)

---

## Розпакування архіву

```bash
tar -xvf mybackups/backup.tar
```

(ТУТ ВСТАВИТИ СКРІНШОТ)

---

## Створення gzip архіву

```bash
tar -czvf backup.tar.gz folder
```

(ТУТ ВСТАВИТИ СКРІНШОТ)

---

## Створення bzip2 архіву

```bash
tar -cjvf backup.tar.bz2 folder
```

(ТУТ ВСТАВИТИ СКРІНШОТ)

---

## Використання grep

```bash
grep "student" file.txt
```

(ТУТ ВСТАВИТИ СКРІНШОТ)

---

## Використання pipe

```bash
cat file.txt | sort
```

(ТУТ ВСТАВИТИ СКРІНШОТ)

---

# Контрольні питання

## Різниця між архівуванням та стисканням

Архівування об'єднує декілька файлів в один архів, а стискання зменшує розмір файлів.

---

## Інші програми архівування

- 7zip  
- rar  
- lzma  
- xz  

---

## Алгоритми стискання

| Алгоритм | Швидкість | Стиснення |
|---|---|---|
| gzip | швидкий | середній |
| bzip2 | середній | хороший |
| xz | повільний | дуже високий |

---

## /dev/null

`/dev/null` — спеціальний файл у Linux, у який можна перенаправити вивід для його ігнорування.

Приклад:

```bash
command > /dev/null
```

---

# Висновок / Conclusion

У ході лабораторної роботи було вивчено команди архівування та стискання файлів у операційній системі Linux. Було розглянуто роботу таких утиліт як `tar`, `gzip`, `bzip2`, `xz` та `zip`. Також було досліджено команди для роботи з текстом (`cat`, `less`, `more`, `head`, `tail`, `grep`).

Крім того, було вивчено принципи роботи потоків введення та виведення, перенаправлення та використання каналів (pipe) у командній оболонці Bash. Отримані знання дозволяють ефективно працювати з файлами, виконувати пошук у текстових даних та автоматизувати обробку інформації у системі Linux.


During this laboratory work we studied the basic Linux commands used for archiving and compressing files such as `tar`, `gzip`, `bzip2`, `xz`, and `zip`. We also explored text processing commands including `cat`, `less`, `more`, `head`, `tail`, and `grep`.

In addition, we learned how input and output streams work, how to use redirection, and how to combine commands using pipes in the Bash shell. These tools allow efficient file management, text analysis, and automation of many tasks in the Linux operating system.
